[package]
name = "dravyn-profile"
version = "0.1.0"
edition = "2024"

[dependencies]
dravyn-common = { version = "0.1.0", path = "../dravyn-common" }
dravyn-network = { version = "0.1.0", path = "../dravyn-network" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
